import { ToClipboardDirective } from './to-clipboard.directive';

describe('ToClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new ToClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
